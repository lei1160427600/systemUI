/**
 * Created by xue on 2017/3/12.
 */
(function(context){
    var $page = $pt.getService(context,'$page')
    $page.model={
        tableModule:[]
    }
}(typeof window !=='undefined' ? window : this ))
